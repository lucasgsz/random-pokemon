# Random Pokemons Specs

Projeto realizado com ReactJs, utilizando a api https://pokeapi.co/

# Instalação

Primeiro, use o `npm install` e depois, `npm start`, esses comandos devem ser realizados na raiz do projeto
